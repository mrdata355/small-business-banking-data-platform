# Local profile writes to ./runtime by default.
OAKBRIDGE_RUNTIME_ROOT=runtime

# Cloud values are intentionally unset. Supply through managed secrets / deployment configuration.
AWS_REGION=us-east-1
DATABRICKS_HOST=
DATABRICKS_TOKEN=
